<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Set Password</title>
  <link rel="stylesheet" href="styles_success_failure.css">
  <!-- CSS only -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-gH2yIJqKdNHPEq0n4Mqa/HGKIhSkIHeL5AyhkYV8i59U5AR6csBvApHHNl/vI1Bx" crossorigin="anonymous">

</head>

<body>
  <!--Navbar Region-->




  <!-- <main>
    <div class="ui segment">
      <h1 style="padding-left: 23rem;padding-top: 5rem;padding-bottom: 3rem;">Set your password for scholar chat</h1>
      <div style="padding-left: 30rem;">
        <input id="pass" type="password" />
        <button id="submitbtn" onclick="submitData()">submit</button>
      </div>

  </main> -->
  <div class="form_wrapper">
    <div class="form_container">
      <div class="title_container">
        <h2>Please Set a Password for Scholar Chat Application</h2>
      </div>
      <div class="row clearfix">
        <div class="">


          <div class="input_field"> <span><i aria-hidden="true" class="fa fa-lock"></i></span>
            <input type="password" id="pass" name="password" placeholder="Password" required />
          </div>
        </div>


        <div class="input_field checkbox_option">
          <input type="checkbox" id="cb1">
          <label for="cb1">I agree with terms and conditions</label>
        </div>

        <input class="button" type="submit" value="Register" onclick="submitData()" />
        
      </div>
    </div>
  </div>
  </div>

  </div>


  <%- include('partials/footer') %>


</body>

</html>

<script>
  // submitData()
  let inputValue;
  document.getElementById("pass").addEventListener('input', function () {
    inputValue = this.value
  })

  async function submitData() {
    // console.log('hit')
    if (inputValue == null) {
      alert('Please enter password')
      return
    }
    dataToSend = inputValue
    // console.log(inputValue)

    let response = await fetch('/sendData', {
      method: 'POST', // or 'PUT'
      headers: {
        'Content-Type': 'application/json',
      },
      body: JSON.stringify({ 'password': dataToSend }),
    })

    // console.log(response.status); // 400
    // console.log(response.statusText); // Not Found

    if (response.status != 200) {
      window.location.href = '/error'
    }
    else {
      window.location.href = '/success'
    }

  }

</script>